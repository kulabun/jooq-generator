package $package;


import javax.annotation.Generated;

import org.jooq.Index;
import org.jooq.OrderField;
import org.jooq.impl.AbstractKeys;


/**
* This class is generated by jooq-generator.
*/
@Generated({"jooq-generator:"})
@SuppressWarnings({"all", "unchecked", "rawtypes"})
public class $className {

#foreach( $table in $schema.tables )
  #foreach( $index in $table.indexes )
    public static final Index ${index.name.toUpperCase()} = Indexes.${index.name.toUpperCase()} ;
  #end
#end

  private static class Indexes extends AbstractKeys {
  #foreach( $table in $schema.tables )
    #foreach( $index in $table.indexes )
    public static Index ${index.name.toUpperCase()} = createIndex("${index.name.toUpperCase()}", ${table.canonicalTableClassName}.${table.name.toUpperCase()}, new OrderField[] { #foreach($column in $index.columns)${table.canonicalTableClassName}.${table.name.toUpperCase()}.${column.name.toUpperCase()}#if( $foreach.hasNext ), #end#end }, ${index.isUnique()});
    #end
  #end
  }

}
